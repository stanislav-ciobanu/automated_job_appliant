# automated_job_appliant
Python scripts for the automated scraping of resident company email addresses from the IT Park website using Selenium (handling cookie acceptance, auto-scrolling, and pop-ups), saving the data to Excel, and automatically sending job application emails—including a CV and certificates (as PDF attachments)—to each company via Gmail SMTP (+spam limit).
